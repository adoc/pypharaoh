"use strict";

/* Config.js for Administration Panel.
*/

define([], function () {
    var Config = {};

    Config.uri = {
        logout: ${json.dumps(request.route_path('auth.logout'))|n},
        api: {
            info: ${json.dumps(request.route_path('api.info'))|n},
            users: ${json.dumps(request.route_path('api.users'))|n},
            groups: ${json.dumps(request.route_path('api.groups'))|n}
            messaging: ${json.dumps(request.route_url('api.messaging'))|n}
        }
    };

    return Config;
});